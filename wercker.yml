box: maven:3-jdk-8

build:
  steps:
    - script:
        name: install
        code: cd limes-core && mvn install -U -Dmaven.wagon.http.ssl.insecure=true -Dmaven.wagon.http.ssl.allowall=true -Dmaven.test.skip=true
    - script:
        name: test
        code: cd limes-core && mvn test
