sudo: false
language: java
jdk:
  - oraclejdk8

addons:
  apt:
    packages:
      - oracle-java8-installer

install: cd limes-core && mvn install -U -Dmaven.wagon.http.ssl.insecure=true -Dmaven.wagon.http.ssl.allowall=true -Dmaven.test.skip=true
script: cd limes-core && mvn test

notifications:
  slack: aksw:OrfNYHb9sxHDlHdndoP7YMDl
